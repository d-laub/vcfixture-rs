name: Release

on:
  workflow_dispatch:
    inputs:
      publish_only:
        description: "Skip bump/tag/GitHub-release; just publish the current Cargo.toml version to crates.io. Use to recover when a prior run bumped and tagged but failed before publishing."
        type: boolean
        default: false

permissions:
  contents: write
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          # Push the bump commit + tag as an admin identity that bypasses the
          # `main` ruleset (PR + required status checks). The default
          # GITHUB_TOKEN is not a bypass actor, so its push to main is rejected.
          token: ${{ secrets.GH_ACTIONS }}

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      # Gate: nothing is bumped, tagged, or published unless these pass.
      - name: test
        run: cargo test --all-features --locked
      - name: package check
        run: cargo publish --dry-run

      # Bump version + changelog + tag. push:false so we own the push and can
      # fold the Cargo.lock sync into the single bump commit.
      - name: Bump version
        id: cz
        if: ${{ !inputs.publish_only }}
        uses: commitizen-tools/commitizen-action@0.23.1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          push: false
          changelog_increment_filename: .release-notes.md

      # commitizen-action is a Docker action running as root, so the objects and
      # files it writes are root-owned and unwritable by the runner user.
      - name: Reclaim workspace ownership
        if: ${{ !inputs.publish_only }}
        run: sudo chown -R "$(id -u):$(id -g)" .

      # commitizen bumps Cargo.toml but not Cargo.lock; sync it and amend it into
      # the bump commit so the committed lockfile is never stale.
      - name: Sync lockfile and push
        if: ${{ !inputs.publish_only }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          cargo update --workspace
          git add Cargo.lock
          git commit --amend --no-edit
          git tag -f "v${{ steps.cz.outputs.version }}"
          git push origin "HEAD:${{ github.ref_name }}"
          git push origin "v${{ steps.cz.outputs.version }}"

      - name: Create GitHub release
        if: ${{ !inputs.publish_only }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "v${{ steps.cz.outputs.version }}" \
            --title "v${{ steps.cz.outputs.version }}" \
            --notes-file .release-notes.md

      - name: Authenticate to crates.io
        id: auth
        uses: rust-lang/crates-io-auth-action@v1

      - name: Publish
        run: cargo publish
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}
